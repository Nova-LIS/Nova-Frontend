import classes from "./ComingSoon.module.css";

const ComingSoon = () => {
    return (
        <div className={classes["container"]}>
            <h1 className={classes["coming-soon"]}>Coming Soon</h1>
        </div>
    );
};

export default ComingSoon;
